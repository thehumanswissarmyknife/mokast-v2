import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.dennisvocke.mokast-v2',
  appName: 'Mokast_v2',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
